kconf non-hardware rt-tune-guest.cfg

if [ "$KARCH" = "x86_64" ]; then
	kconf non-hardware rt-tune-guest-x86_64.cfg
fi

if [ "$KMACHINE" != "ti-j78xx" ]; then
	kconf non-hardware rt-tune-additional.cfg
fi
