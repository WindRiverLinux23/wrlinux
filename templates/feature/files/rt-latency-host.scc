kconf non-hardware rt-latency-host.cfg

if [ "$KARCH" = "x86_64" ]; then
	kconf non-hardware rt-latency-host-x86_64.cfg
fi

if [ "$KMACHINE" != "ti-j78xx" ]; then
	kconf non-hardware rt-latency-additional.cfg
fi
